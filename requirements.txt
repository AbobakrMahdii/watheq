# =============================================================================
# Watheq Project — Unified Python Requirements
# Generated: 2026-02-08
# =============================================================================

# ── Web Framework & Server ──────────────────────────────────────────────────
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
email-validator>=2.0.0

# ── Authentication & Security ───────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ── Database (MySQL) ────────────────────────────────────────────────────────
databases>=0.9.0
aiomysql>=0.2.0

# ── AI / Deep Learning (v3 — ElementClassifier + FontAnalyzer) ─────────────
# NOTE: torch and torchvision are NOT installed via this file.
# They require a special install command for GPU (CUDA) support.
# Run:  scripts\setup_python.bat   (auto-detects GPU vs CPU)
# Or manually:
#   GPU:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   CPU:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
numpy>=1.24.0
opencv-python>=4.5.0             # imported as cv2
Pillow>=9.0.0

# ── Biometric (Face Matching) ──────────────────────────────────────────────
deepface>=0.0.79
tf-keras                         # required by deepface

# ── OCR ─────────────────────────────────────────────────────────────────────
google-cloud-vision>=3.0.0       # Google Vision API for OCR
PyMuPDF>=1.23.0                  # imported as fitz (PDF processing)

# ── Blockchain / IPFS ──────────────────────────────────────────────────────
requests>=2.28.0                 # HTTP calls to MultiChain & IPFS

# ── Report Export ───────────────────────────────────────────────────────────
openpyxl>=3.1.0                  # Excel export
fpdf2>=2.7.0                     # PDF export (imported as fpdf)

# ── Configuration ───────────────────────────────────────────────────────────
python-dotenv>=1.0.0             # imported as dotenv
PyYAML>=6.0                      # imported as yaml

# ── AI Training Utilities (optional — only needed for model training) ──────
scikit-image>=0.19.0
scikit-learn>=1.0.0
albumentations>=1.3.0
matplotlib>=3.5.0
seaborn>=0.12.0
pandas>=2.0.0
tqdm>=4.64.0

# NOTE: System-level dependencies not covered by pip:
#   - NVIDIA GPU driver (for CUDA-accelerated torch — optional)
#   - Google Cloud credentials JSON (for Vision OCR)
#   - MultiChain daemon (multichaind) running locally
#   - IPFS daemon (kubo) running locally
#
# NOTE: ultralytics (YOLOv8) was removed in AI v3.
#   Element detection now uses layout_config.yaml ROIs.
#   Verification uses trained ElementClassifier + FontAnalyzer.
#
# SETUP: Run scripts\setup_python.bat to install ALL dependencies
#   including PyTorch with automatic GPU/CPU detection.
